/* eslint-disable react/prop-types */
// import { useDrag } from "@react-three/drei";

// function Draggable({ children, onDrag, onDragEnd }) {
//   const bind = useDrag(
//     (props) => {
//       onDrag && onDrag(props.offset);
//     },
//     {
//       onEnd: (props) => {
//         onDragEnd && onDragEnd(props.offset);
//       },
//     }
//   );

//   return <group {...bind()}>{children}</group>;
// }

// export default Draggable;
